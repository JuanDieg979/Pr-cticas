# Aprender a usar GitHub X Git
---
Este repositorio sera para saber como funciona git x github y saber para que sirve cada comando
- `git init`: Genera un nuevo repositorio en la carpeta actual
- `git clone`: Descarga un repositorio remoto (por ejemplo de GitHub) a tu computadora.
- `git status`: Muestra el estado de los archivos: cuáles cambiaron, cuáles están listos para commit, etc.
- `git add`: Agrega un archivo al "staging area" (zona de preparación antes del commit).
- `git add .`: Agrega todos los archivos modificados.